# Reference

`all_cos_phi3_authors.pkl` — copied from the original repo's
`Code/Cos_sim_analysis/cos_sims/all_cos.pkl`. **This file has only 10 draws
per pair-type, not the paper's r=500** — see `docs/KNOWN_DISCREPANCIES.md` #3.
Use it as a pipeline sanity check (does your `compare.py` output look
sane against *something*?), not as the ground-truth target for the
replication log.

Source: https://github.com/listen0425/Safety-Layers
